---
import Layout from '../layouts/Layout.astro';
import MapView from '../components/MapView.jsx';
import Dashboard from '../components/Dashboard.jsx';
import QueryBar from '../components/QueryBar.jsx';
import AlertsPanel from '../components/AlertsPanel.jsx';
import RecommendationCard from '../components/RecommendationCard.jsx';
import HealthAdvisory from '../components/HealthAdvisory.jsx';
import WardDetailPanel from '../components/WardDetailPanel.jsx';
import ActionCenter from '../components/ActionCenter.jsx';
import CitizenReport from '../components/CitizenReport.jsx';
---

<Layout>
  <div class="p-4 sm:p-6 lg:p-8">
    <div class="max-w-[1600px] mx-auto space-y-8 lg:space-y-10">

      <!-- Query Bar -->
      <QueryBar client:load />

      <!-- Map: Hero section - full width, dominant -->
      <section aria-label="Traffic Map" class="relative">
        <div class="flex items-center gap-2 mb-4">
          <span class="w-1 h-5 rounded-full bg-[#31D0AA] shadow-[0_0_8px_rgba(49,208,170,0.6)]"></span>
          <h2 class="text-lg font-bold" style="color: var(--amber)">Traffic Map</h2>
          <span class="eyebrow ml-auto hidden sm:block">Bengaluru · Live Network</span>
        </div>
        <div class="relative">
          <MapView client:only="react" />
          <div class="pointer-events-none absolute bottom-0 left-0 right-0 h-16 bg-gradient-to-t from-[#0B0E14] to-transparent rounded-b-[16px]"></div>
        </div>
      </section>

      <!-- Second domain: Environmental / Community Wellness -->
      <section aria-label="Health & Air-Quality Advisories">
        <HealthAdvisory client:load />
      </section>

      <!-- Live Telemetry -->
      <section aria-label="Live Telemetry">
        <div class="flex items-center gap-2 mb-4">
          <span class="w-1 h-5 rounded-full bg-[#31D0AA] shadow-[0_0_8px_rgba(49,208,170,0.6)]"></span>
          <h2 class="text-lg font-bold" style="color: var(--amber)">Live Telemetry</h2>
        </div>
        <Dashboard client:load />
      </section>

      <!-- Alerts + AI Recommendations -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 lg:gap-8">
        <AlertsPanel client:load />
        <RecommendationCard client:load />
      </div>

      <!-- Phase 2: Environment Ward Details -->
      <section aria-label="Ward Livability">
        <h2 class="text-lg font-bold text-[#31D0AA] mb-4 flex items-center gap-2">
          <span class="w-1 h-5 rounded-full bg-[#10b981]"></span>
          Ward Livability Score
        </h2>
        <WardDetailPanel client:load />
      </section>

      <!-- Phase 3: Action Center & Citizen Reporting -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <section aria-label="Action Center">
          <ActionCenter client:load />
        </section>
        
        <section aria-label="Citizen Reporting">
          <CitizenReport client:load />
        </section>
      </div>
    </div>
  </div>
</Layout>
